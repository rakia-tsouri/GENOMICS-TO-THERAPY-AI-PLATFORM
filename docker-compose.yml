services:
  genomics-validation:
    build:
      context: .
      dockerfile: genomics_validation/Dockerfile
    container_name: genomics-validation
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    networks:
      - bio-network

  protein-analysis-service:
    build:
      context: .
      dockerfile: protein_analysis_service/Dockerfile
    container_name: protein-analysis-service
    ports:
      - "8001:8001"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - USE_BLAST=true
      - DATA_PATH=/data/structures
    volumes:
      - bio-data:/data/structures
    depends_on:
      - redis
    networks:
      - bio-network

  redis:
    image: "redis:alpine"
    container_name: bio-redis
    ports:
      - "6379:6379"
    networks:
      - bio-network

  drug-discovery-service:
    build:
      context: ./drug_discovery_service
      dockerfile: Dockerfile
    container_name: drug-discovery-service
    ports:
      - "8002:8002"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CHEMBL_API_URL=https://www.ebi.ac.uk/chembl/api/data
      - MODEL_PATH=/app/models_saved/gnn_v1.pt
      - PDB_VOLUME=/data/structures
    volumes:
      - bio-data:/data/structures
    depends_on:
      - redis
    networks:
      - bio-network

networks:
  bio-network:
    driver: bridge

volumes:
  bio-data:
    driver: local
